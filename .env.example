# X-SIDECHAIN reads secrets from the process environment and never writes them
# to its configuration or audit files.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
OPENROUTER_API_KEY=

# Add any other variable name referenced by a provider's auth.env field.
# Models and agent count are configured in x-sidechain.json.
