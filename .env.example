# X-SIDECHAIN reads secrets from the process environment and never writes them
# to its configuration or audit files.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=

# Optional model overrides.
OPENAI_MODEL=gpt-6-astra
ANTHROPIC_MODEL=claude-sonnet-5
XAI_MODEL=grok-4.6
